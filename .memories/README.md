# Memories index

Project knowledge store: one fact per file, grep-friendly, committed and shared.
Each file opens with `description:` frontmatter. This index is auto-generated by
`tools/memory-index.ps1` (run by the pre-commit hook) -- do not hand-edit the list.

- [asr-gpu-install-command-shape](asr-gpu-install-command-shape.md) -- The GUI's ASR/GPU install command — one --extra-index-url line, cu128, and the four footguns behind that shape
- [decoder-tool-resolution](decoder-tool-resolution.md) -- Decoder tool paths are set at RUNTIME by config.apply_tool_env, so a shell check for DECIWAVES_VGMSTREAM or a PATH lookup reports "no decoder installed" on a fully configured machine -- ask `deciwaves doctor`, never the shell
- [ds1-story-reel-composition](ds1-story-reel-composition.md) -- Measured composition of the DS1 --main-story reel: 60% of runtime is cutscene, 40% is main-cast codec calls, and ~8% is subtitled system/ambient pollution the subtitle_en filter cannot catch
- [ds2-audio-binding](ds2-audio-binding.md) -- DS2 dialogue audio is Wwise .wem (RIFF fmt 0xFFFF, mono) inside DSAR containers that the repo's existing FwStreamStore reads unmodified; its 12-locator block IS FW's 12 languages with only slot 0 installed, and DS2 ships parseable SentenceResource/LocalizedTextResource
- [ds2-gamescript-binding](ds2-gamescript-binding.md) -- A BYO DS2 fan gamescript binds 40.7% of DS2's clips (MEASURED 2026-08-02, #386 -- not the ~48% first predicted) to a speaker and a story position via ASR + sentence-level fuzzy match, unblocking story order and speakers WITHOUT the blocked object reader, provided matching is per sentence not per speaker turn; the shortfall is missing audio in ~7 sections, not a matcher defect
- [ds2-nonriff-locators](ds2-nonriff-locators.md) -- DS2's 7 non-RIFF slot-0 locators are a 2-byte skew (identical `f1 10` prefix, valid Wwise header at offset+2), not corruption -- but reading from +2 still fails to decode because the chunk after `fmt ` is misaligned by 2 bytes
- [ds2-object-reader](ds2-object-reader.md) -- DS2's object serialization is FW's with exactly three deltas -- a 27-entry LocalizedTextResource trailer, a 3-byte per-entry suffix, and a new 8-byte StringHash atom -- taking GroupReader from 0/1378 to 803/1378 and yielding real exact subtitles
- [ds2-story-order-signals](ds2-story-order-signals.md) -- DS2 story order has a real, validated coarse signal that needs no object reader -- the l###_xxx package directory names are numbered story regions (l100_mex early ... l700_bea endgame), confirmed by transcribing clips from each; but 91.5% of lines sit in just two of them, so intra-region group ordering is still unsolved
- [ds2-streaming-graph](ds2-streaming-graph.md) -- Death Stranding 2's streaming_graph.core is Forbidden West's StreamingGraphResource with exactly two layout deltas -- a 68-byte group stride and a trailing GGUUID array -- but only 23% of its RTTI type hashes are shared with FW
- [ds2-types-json](ds2-types-json.md) -- A DS2 types.json with 100% graph-type coverage exists (odradek ships one, same source as the FW file already in use) -- but it does NOT unblock Phase 3 on its own, because GroupReader fails on 100% of DS2 dialogue groups while succeeding on FW
- [ds-curated-flag-lets-in-extra-cutscenes](ds-curated-flag-lets-in-extra-cutscenes.md) -- `ds render --curated` bypasses NON_STORY_CS_GROUPS by design, so Extra/Battlefield cutscene groups flow into a 'story' cut and can even END it — an external curator must drop them itself, and must read the group set from episode_map rather than listing groups by hand
- [ds-cutscene-audio](ds-cutscene-audio.md) -- DS story cutscenes are real-time in-engine (not pre-rendered video), their dialogue audio is a handful of per-scene Wwise voice tracks rather than per-line clips, and the grunt/dead-air trim is a speech-region-keep technique, not silence detection
- [ds-data-dir-is-the-data-subdir](ds-data-dir-is-the-data-subdir.md) -- `--data-dir` for DS stages must be <install>/data, not the install root; the root has zero .bin files so PackIndex builds an EMPTY index and blames each individual stream — and a warm wav-cache disguises it as a category-specific format bug
- [ds-dialogue-is-variants-not-conversation](ds-dialogue-is-variants-not-conversation.md) -- DS `terminal` scenes are mutually-exclusive response VARIANTS mixed with real interview content, and `npc` is six per-character BARK BANKS — neither is a conversation, so scene-level keep/drop is the wrong unit and text-similarity dedupe does not find the redundancy
- [ds-keepspans-silently-undertrims](ds-keepspans-silently-undertrims.md) -- ds/cutscene-keepspans.csv is WhisperX ASR output consumed blind by the renderer, so where ASR under-detects speech the render silently trims real dialogue away — 6 DS1 scenes lose ~12 min including the game's opening narration, with no error anywhere
- [ds-render-honours-playlist-order](ds-render-honours-playlist-order.md) -- `ds render` renders playlist.csv rows verbatim in file order — but ONLY on the --single-file path; the multi-reel path packs WHOLE EPISODES in ascending order and silently undoes any cross-episode reordering
- [ds-speech-spans-multiple-lines](ds-speech-spans-multiple-lines.md) -- DS splits one spoken speech across several sentence-level lines, so ANY per-line filter (score threshold, length rule, keyword match) punches holes through monologues — selection must operate on passages, and a lone continuation fragment is the audible stub
- [ds-speech-trim-is-load-bearing](ds-speech-trim-is-load-bearing.md) -- DS render without --speech-trim adds ~51 minutes of pure dead air (measured); the keep-spans map is BUNDLED in the repo, and the DS workspace's other intermediates are regenerable/packaged too
- [ds-transcript-anchoring-is-the-real-ordering](ds-transcript-anchoring-is-the-real-ordering.md) -- DS's default playlist order is the UN-anchored fallback; `ds order --transcript <gamescript>` is the real ordering and fixes the opening, but low-text scenes anchor unreliably within a cutscene group
- [ds-wwise-wem-format](ds-wwise-wem-format.md) -- DS:DC dialogue is described by Decima resources but the encoded audio payload is Wwise .wem, not Decima-native audio — both layers have to be parsed
- [fw-streaming-graph](fw-streaming-graph.md) -- Forbidden West ships a positional streaming-graph index that HZD Remastered lacks, so its per-line audio binding is resolved by replaying deserialization order rather than by content fingerprinting
- [fw-subtitle-binding](fw-subtitle-binding.md) -- Forbidden West's in-game subtitles are readable per group but not positionally paired to their audio clip — recovered via a bounded within-group fuzzy match against ASR instead of a global external-script match
- [game-package-subdirs](game-package-subdirs.md) -- The HZD/FW "package" paths are subdirectories of the install, not the install root - LocalCacheDX12\package (HZDR) and LocalCacheWinGame\package (HFW)
- [gui-export-output-dirs](gui-export-output-dirs.md) -- The GUI's rendered-reel output directory is NOT a uniform out/<game>/ — it's out/audio (DS), out/hzd/audio (HZD), out/fw/reels (FW); job_controller.py's own success message is the source of truth
- [gui-modal-dialog-headless-hang](gui-modal-dialog-headless-hang.md) -- A Qt modal dialog (QMessageBox.question/exec) reachable from a teardown path hangs headless pytest-qt CI forever — no per-test timeout means it burns ~4-6h before the runner kills it
- [gui-qsettings-test-isolation](gui-qsettings-test-isolation.md) -- #128's QSettings persistence makes every MainWindow() test read/write the REAL registry scope — local pytest scribbles test state into the user's actual GUI prefs; needs an autouse isolation fixture
- [gui-strict-branch-protection](gui-strict-branch-protection.md) -- main now requires the `test` check + up-to-date branches (strict) as of 2026-07-21 (#184); foreman-status merges must serialize (update-branch → CI → merge per PR), not batch.
- [hzd-pack-format](hzd-pack-format.md) -- HZD Remastered ships the newer Forbidden-West-generation package format (unencrypted DSAR archives + a path-hash index), not DS's encrypted packfile — a different reader is required
- [hzd-structural-binding](hzd-structural-binding.md) -- HZD's per-line audio binding uses a structural content fingerprint (encoded byte length + decoded sample count) to bucket clips against catalog lines, falling back to a small, targeted ASR pass only for buckets with more than one candidate
- [release-process](release-process.md) -- How to cut a DeciWaves release — tag v* triggers OIDC publish to PyPI, no API token exists
- [toolchain-notes](toolchain-notes.md) -- Which third-party tools each game's audio pipeline actually needs and why — Wwise vs ATRAC9 decoders, an RTTI type-table reference for Forbidden-West-generation resources, and where ASR sits as an optional, GPU-gated stage
- [worktree-editable-install-contamination](worktree-editable-install-contamination.md) -- Parallel git worktrees share one editable-install .pth and race over it; pytest can import the wrong worktree's src unless pyproject pins pythonpath=["src"]
