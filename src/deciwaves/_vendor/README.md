# Vendored dependencies

- `pydecima/` — MIT-licensed Decima `.core` reader (see its LICENSE), bundled with
  the Death Stranding PC patch from `patches/pydecima-dspc.patch` pre-applied and
  imports rewritten to `deciwaves._vendor.pydecima`.
